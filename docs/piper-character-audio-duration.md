# Piper character audio duration

Character PCM duration and sustainable key-repeat rate remain open
measurements. This experiment does not trim silence or time-compress audio:
those changes require voice-neutral intelligibility evidence. The bounded
queue prevents unbounded PCM retention during held-key input.
